<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.istream</groupId>
        <artifactId>istream</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>istream-app</artifactId>
    <name>istream-app</name>
    <description>Runnable Spring Boot application entry point</description>

    <dependencies>
        <dependency>
            <groupId>com.istream</groupId>
            <artifactId>istream-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.istream</groupId>
            <artifactId>istream-sources</artifactId>
        </dependency>
        <dependency>
            <groupId>com.istream</groupId>
            <artifactId>istream-consumers</artifactId>
        </dependency>
        <dependency>
            <groupId>com.istream</groupId>
            <artifactId>istream-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.istream</groupId>
            <artifactId>istream-persistence</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <!-- Embedded Kafka for local profile (compile scope so LocalKafkaInitializer loads it).
             spring-kafka-test's KRaft broker transitively calls junit-jupiter-api.Assertions at
             runtime, so junit-jupiter-api must be compile-scope too. -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>compile</scope>
        </dependency>
        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
